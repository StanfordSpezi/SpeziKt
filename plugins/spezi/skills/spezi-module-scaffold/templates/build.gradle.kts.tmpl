// Template for a Spezi-Kotlin module's build.gradle.kts.
// Replace <package-segment> with the dotted package suffix (e.g., "account.firebase" for module "account-firebase").
// Add or remove convention plugins based on what the module needs.
// Keep dependency declarations in `project(":x")` string form; do not introduce typesafe project accessors here.

plugins {
    alias(libs.plugins.spezi.library)
    // alias(libs.plugins.spezi.compose)         // uncomment when the module ships Compose UI
    // alias(libs.plugins.spezi.hilt)            // uncomment when the module participates in Hilt DI
    // alias(libs.plugins.spezi.serialization)   // uncomment when the module uses kotlinx.serialization
}

android {
    namespace = "edu.stanford.spezi.<package-segment>"
}

dependencies {
    // api(project(":foundation"))
    // implementation(project(":core"))
    // androidTestImplementation(project(":testing-ui"))
}
